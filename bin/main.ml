open Emojifier

let () = Emojifier.test
(*let () = Scroll.scroll_one_frame 
  1
  Mock.sample_arrangement
  (0.5, 0.5)
  0.69 |> ignore

let () = print_endline (
  (Mock.sample_arrangement |> List.hd).file_path 
  |> Option.get
)*)

